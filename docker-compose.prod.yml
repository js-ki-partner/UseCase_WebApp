# Produktions-Compose fuer den IONOS-VPS. Laeuft neben OpenProject hinter
# demselben Caddy-Reverse-Proxy.
#
#   sops -d --output-type dotenv secrets.enc.yaml > .env
#   docker compose -f docker-compose.prod.yml up -d --build
#
# .env wird bei jedem Deploy aus secrets.enc.yaml generiert (siehe DEPLOYMENT.md
# / BETRIEB.md) und ist git-ignoriert.
services:
  app:
    build:
      context: .
    image: ucradar:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3005:3000" # nur lokal fuer Healthcheck/Debug; Traffic laeuft ueber proxy-net
    volumes:
      - ./openproject-mapping.json:/app/openproject-mapping.json:ro
    depends_on:
      - db
    networks:
      default: {}
      proxy-net:
        aliases:
          - ucradar # Caddy proxyt auf diesen Namen (siehe Caddyfile.snippet)

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ucradar}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD fehlt}
      POSTGRES_DB: ${POSTGRES_DB:-ucradar}
    volumes:
      - ucradar-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ucradar}"]
      interval: 10s
      timeout: 5s
      retries: 10

networks:
  proxy-net:
    external: true # von der Caddy-Stack angelegt, geteilt mit OpenProject

volumes:
  ucradar-db-data:
